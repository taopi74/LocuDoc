/** Shared motion tokens — Kinetic Precision micro-interaction scale. */
export const Motion = {
  fast: 160,
  normal: 280,
  slow: 420,
  stagger: 55,
  spring: { damping: 18, stiffness: 220 },
} as const;
